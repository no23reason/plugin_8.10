// (C) 2022 GoodData Corporation
import React from "react";
import { FormattedMessage } from "react-intl";
import { LoadingSpinner } from "@gooddata/sdk-ui-kit";
import { ScheduledEmail } from "./ScheduledEmail";
export var ScheduledEmails = function (props) {
    var isLoading = props.isLoading, scheduledEmails = props.scheduledEmails, currentUserEmail = props.currentUserEmail, onDelete = props.onDelete;
    if (isLoading) {
        return React.createElement(LoadingSpinner, { className: "gd-scheduled-emails-loading-spinner large" });
    }
    if (scheduledEmails.length === 0) {
        return (React.createElement("div", { className: "gd-scheduled-emails-message s-no-schedules-message" },
            React.createElement(FormattedMessage, { id: "dialogs.schedule.management.noSchedules", values: { br: React.createElement("br", null) } })));
    }
    return (React.createElement(React.Fragment, null, scheduledEmails.map(function (scheduledEmail) { return (React.createElement(ScheduledEmail, { key: scheduledEmail.identifier, scheduledEmail: scheduledEmail, currentUserEmail: currentUserEmail, onDelete: onDelete })); })));
};
//# sourceMappingURL=ScheduledEmails.js.map