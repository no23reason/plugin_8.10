export function loadAvailableDisplayFormRefs(ctx, displayForms) {
    var backend = ctx.backend, workspace = ctx.workspace;
    return backend
        .workspace(workspace)
        .attributes()
        .getAttributeDisplayForms(displayForms)
        .then(function (references) { return references.map(function (df) { return df.ref; }); });
}
//# sourceMappingURL=loadAvailableDisplayFormRefs.js.map