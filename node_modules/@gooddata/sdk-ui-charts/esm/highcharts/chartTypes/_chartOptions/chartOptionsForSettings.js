// (C) 2007-2022 GoodData Corporation
import { __assign } from "tslib";
/**
 * @internal
 */
export function updateConfigWithSettings(config, settings) {
    var updatedConfig = config;
    if (settings) {
        if (settings["disableKpiDashboardHeadlineUnderline"] === true) {
            updatedConfig = __assign(__assign({}, updatedConfig), { disableDrillUnderline: true });
        }
        if (settings["enableKDWidgetCustomHeight"] === true) {
            updatedConfig = __assign(__assign({}, updatedConfig), { enableCompactSize: true });
        }
        if (updatedConfig === undefined || updatedConfig.enableJoinedAttributeAxisName === undefined) {
            updatedConfig = __assign(__assign({}, updatedConfig), { enableJoinedAttributeAxisName: settings["enableAxisNameViewByTwoAttributes"] });
        }
        if (settings.enableChartsSorting) {
            updatedConfig = __assign(__assign({}, updatedConfig), { enableChartSorting: true });
        }
    }
    return updatedConfig;
}
//# sourceMappingURL=chartOptionsForSettings.js.map